var fs = require('fs');
// open the file in read / write mode
var dataUpdates = require("../../data/updates.json");
console.log(dataUpdates);
var dataEntList = require("../../data/entList.json");
console.log(dataEntList);
